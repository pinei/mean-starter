{
  "name": "mean-starter",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "build:client": "cd client && npm install",
    "build:server": "cd server && npm install",
    "preinstall": "npm run build:client && npm run build:server",
    "start": "node ./bin/www"
  }
}
